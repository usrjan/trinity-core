<?php

/**
 * МИНИМАЛЬНЫЙ ФАЙЛ МАРШРУТОВ
 * 
 * Этот файл содержит только самые базовые маршруты,
 * которые нужны для запуска системы.
 * 
 * Все остальные маршруты загружаются из базы данных
 * (нейроны type='route').
 * 
 * === ПОРЯДОК ЗАГРУЗКИ ===
 * 1. Файловые маршруты (этот файл)
 * 2. Маршруты из базы (через Kernel::loadRoutes())
 * 
 * Маршруты из базы сортируются по sort,
 * поэтому они автоматически получают правильный приоритет.
 * 
 * === ДЛЯ МЕНЯ ===
 * Этот файл почти пустой. И это хорошо.
 * В старом Trinity routes.php был огромным.
 * Теперь всё в базе. Ядро чистое.
 * 
 * === КНИГА ===
 * Глава 8. Карта.
 * 
 * Каждая Тень имеет свою карту.
 * Этот файл — карта ядра. Минимальная. Только самое важное.
 */

use Symfony\Component\Routing\Route;
use Symfony\Component\Routing\RouteCollection;

$routes = new RouteCollection();

// Единственный маршрут в файле — главная страница.
// Он нужен чтобы система запустилась даже если база пуста.
$routes->add('home', new Route('/', [
	'_controller' => Jan\Trinity\Plugin\Spa\HomeController::class,
	'_method'     => 'index',
]));

// Динамические страницы — загружаются из базы

return $routes;